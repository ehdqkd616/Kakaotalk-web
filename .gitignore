# ── 설계 문서 (로컬 전용) ──
설계서.md
설계서.docx

# ── 환경 변수 (민감 정보) ──
.env

# ── 의존성 ──
node_modules/
.pnp
.pnp.js

# ── 빌드 결과물 ──
.next/
out/
dist/
build/

# ── 환경 변수 ──
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# ── 로그 ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
auth-error.log

# ── DB 데이터 ──
server/data/

# ── 에디터 ──
.vscode/settings.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── TypeScript ──
*.tsbuildinfo
next-env.d.ts

# ── 테스트 커버리지 ──
coverage/
.nyc_output/

# ── 캐시 ──
.eslintcache
.cache/
.turbo/
